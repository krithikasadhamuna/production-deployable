# OPTIMIZED REQUIREMENTS - Balanced for AI SOC Platform
# This includes LangChain but avoids heavy optional dependencies

# Core Flask
flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7

# Database
aiosqlite==0.19.0

# Authentication
pyjwt==2.8.0
bcrypt==4.1.2

# ML/AI Dependencies (Required for detection)
scikit-learn==1.3.0
joblib==1.3.2
numpy==1.24.3
pandas==2.0.3

# LangChain Core (ESSENTIAL for AI Agents)
langchain==0.3.0
langchain-community==0.3.0
# LangGraph for stateful workflows
langgraph==0.6.7

# Core dependencies that LangChain needs
pydantic>=2.0
tenacity>=8.0
aiohttp>=3.8
requests>=2.31
PyYAML>=6.0

# Utilities
psutil==5.9.5
python-dateutil==2.8.2
python-dotenv==1.0.0

# WHY LANGCHAIN IS NEEDED:
# - AI Attack Agent: Uses LangChain for attack planning with CyberSecAI
# - AI Detection Agent: Uses LangChain for threat analysis
# - LangGraph workflows: Orchestrates multi-step AI operations
# - Human-in-the-loop: Manages approval workflows
# Without LangChain, the AI agents won't work!
